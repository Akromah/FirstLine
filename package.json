{
  "name": "firstline-monorepo",
  "private": true,
  "version": "0.1.0",
  "description": "Modern CAD platform with Python backend and web client",
  "workspaces": [
    "apps/web"
  ],
  "scripts": {
    "dev:web": "npm run dev --workspace @firstline/web",
    "build:web": "npm run build --workspace @firstline/web",
    "lint:web": "npm run lint --workspace @firstline/web"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
